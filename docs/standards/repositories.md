# Repository Layout

A standard repository follows this structure.

```text
docs/
examples/
packages/
scripts/
tests/
```

Each top-level directory has a single responsibility.

The same layout should be used consistently across repositories whenever applicable.
```
